policies:
  - name: ec2-stopped-instances-terminate
    resource: ec2
    description: |
      Terminate EC2 instances that have been stopped > 30 days.
    filters:
      - "State.Name": stopped
      - type: state-age
        op: ge
        days: 30
    actions:
      - type: terminate
        true
