policies:
  - name: cloudwatch-log-retention-never-expire
    resource: log-group
    description: |
      Finds CloudWatch Log Groups with retention set to
      never expire and make them expire in 90 days.
    filters:
      - type: value
        key: retentionInDays
        value: absent
    actions:
      - type: retention
          days: 90
